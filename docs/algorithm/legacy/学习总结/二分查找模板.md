---
title: "二分查找模板"
description: "二分查找模板 的历史学习笔记。"
date: 2026-05-21
tags: ["算法", "数据结构"]
keywords: ["算法", "数据结构", "二分查找模板"]
legacy_source: "基础/数据结构与算法/学习总结/二分查找模板.md"
---
#### 二分查找模板

[labuladong二分查找算法详解](https://leetcode-cn.com/problems/find-first-and-last-position-of-element-in-sorted-array/solution/er-fen-cha-zhao-suan-fa-xi-jie-xiang-jie-by-labula/)

一、寻找一个数（基本的二分搜索）

```java
int binarySearch(int[] nums, int target) &#123;
    int left = 0;
    int right = nums.length - 1;
    while (left &lt;= right) &#123;
        int mid = left + (right - left) / 2;
        if (nums[mid] == target) &#123;
            return mid;
        &#125; else if (nums[mid] &lt; target) &#123;
            left = mid + 1;
        &#125; else if (nums[mid] > target) &#123;
            right = mid - 1;
        &#125;
    &#125;
    return -1;
&#125;
```

二、寻找左侧边界的二分搜索

开区间写法：
```java
int leftBound(int[] nums, int target) &#123;
    if (nums.length == 0) return -1;
    int left = 0;
    int right = nums.length;
    
    while (left &lt; right) &#123;
        int mid = left + (right - left) / 2;
        if (nums[mid] == target) &#123;
            right = mid;
        &#125; else if (nums[mid] &lt; target) &#123;
            left = mid  + 1;
        &#125; else if (nums[mid] > target) &#123;
            right = mid;
        &#125;
    &#125;
    
    // 退出循环时left==right==nums.length会越界
    if (left == nums.length) return -1;
    return nums[left] == target ? left : -1;
&#125;
```

闭区间写法：
```java
int leftBound(int[] nums, int target) &#123;
    if (nums.length == 0) return -1;
    int left = 0;
    int right = nums.length - 1;
    
    while (left &lt;= right) &#123;
        int mid = left + (right - left) / 2;
        if (nums[mid] == target) &#123;
            right = mid - 1;
        &#125; else if (nums[mid] &lt; target) &#123;
            left = mid + 1；
        &#125; else if (nums[mid] > target) &#123;
            right = mid - 1;
        &#125;
    &#125;
    
    
&#125;
```
