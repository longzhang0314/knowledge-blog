---
title: "堆"
description: "堆 的历史学习笔记。"
date: 2026-05-21
tags: ["算法", "数据结构"]
keywords: ["算法", "数据结构", "堆"]
legacy_source: "基础/数据结构与算法/刷题总结/堆.md"
---
#### 堆

**355. 设计推特**

设计一个推特，功能如下：

postTweet(userId, tweetId): 创建一条新的推文

getNewsFeed(userId): 检索最近的十条推文。每个推文都必须是由此用户关注的人或者是用户自己发出的。推文必须按照时间顺序由最近的开始排序。

follow(followerId, followeeId): 关注一个用户

unfollow(followerId, followeeId): 取消关注一个用户

---

首先需要一个推文节点，属性带有id和创建时间戳；使用链表节点实现。

需要有一个字典表存储用户id和推文链表；

需要有一个字典表存储用户id和他关注的用户id列表；

需要一个全局的时间戳在插入时使用。

获取最新的10条推文（包含关注人）时，需要把用户自己和关注人的所有推文按照时间戳放入一个大小为10的小顶堆中（减少堆化的影响），然后取出addFirst插入链表头部返回。

```java
class Twitter &#123;

    private Map&lt;Integer, Tweet> twitter;
    private Map&lt;Integer, Set&lt;Integer>> followings;
    private int timestamp;

    /**
     * Initialize your data structure here.
     */
    public Twitter() &#123;
        this.twitter = new HashMap&lt;>();
        this.followings = new HashMap&lt;>();
    &#125;

    /**
     * Compose a new tweet.
     * 最新的插入链表头部，头部有个虚节点
     */
    public void postTweet(int userId, int tweetId) &#123;
        timestamp++;
        Tweet head = twitter.getOrDefault(userId, new Tweet(-1, -1));
        Tweet cur = new Tweet(tweetId, timestamp);
        cur.next = head.next;
        head.next = cur;
        twitter.put(userId, head);
    &#125;

    /**
     * Retrieve the 10 most recent tweet ids in the user's news feed. Each item in the news feed must be posted by users who the user followed or by the user herself. Tweets must be ordered from most recent to least recent.
     */
    public List&lt;Integer> getNewsFeed(int userId) &#123;
        LinkedList&lt;Integer> res = new LinkedList&lt;>();
        Set&lt;Integer> set = followings.get(userId);
        // 小顶堆存储，减少堆化次数
        PriorityQueue&lt;Tweet> heap = new PriorityQueue&lt;>(10, Comparator.comparingInt(o -> o.timestamp));
        addHeap(heap, userId);
        if (set != null && set.size() != 0) &#123;
            for (int followeeId : set) &#123;
                addHeap(heap, followeeId);
            &#125;
        &#125;

        while (!heap.isEmpty()) &#123;
            res.addFirst(heap.poll().id);
        &#125;
        return res;
    &#125;

    // 一个链表的数插入heap
    private void addHeap(PriorityQueue&lt;Tweet> heap, int userId) &#123;
        int count = 0;
        Tweet tweet = twitter.get(userId);
        while (tweet != null && tweet.next != null && ++count &lt;= 10) &#123;
            add(heap, tweet.next);
            tweet = tweet.next;
        &#125;
    &#125;

    // 单个节点插入堆
    private void add(PriorityQueue&lt;Tweet> heap, Tweet t) &#123;
        if (heap.size() == 10 && heap.peek().timestamp &lt; t.timestamp) &#123;
            heap.poll();
            heap.offer(t);
        &#125; else if (heap.size() &lt; 10) &#123;
            heap.offer(t);
        &#125;
    &#125;


    /**
     * Follower follows a followee. If the operation is invalid, it should be a no-op.
     */
    public void follow(int followerId, int followeeId) &#123;
        if (followerId == followeeId) return;
        Set&lt;Integer> set = followings.getOrDefault(followerId, new HashSet&lt;>());
        set.add(followeeId);
        followings.put(followerId, set);
    &#125;

    /**
     * Follower unfollows a followee. If the operation is invalid, it should be a no-op.
     */
    public void unfollow(int followerId, int followeeId) &#123;
        if (followings.containsKey(followerId)) &#123;
            Set&lt;Integer> set = followings.get(followerId);
            set.remove(followeeId);
        &#125;
    &#125;

    private static class Tweet &#123;
        private int id;
        private int timestamp;
        private Tweet next;

        Tweet(int id, int timestamp) &#123;
            this.id = id;
            this.timestamp = timestamp;
        &#125;
    &#125;
&#125;
```
