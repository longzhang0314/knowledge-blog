---
title: "双指针"
description: "双指针 的历史学习笔记。"
date: 2026-05-21
tags: ["算法", "数据结构"]
keywords: ["算法", "数据结构", "双指针"]
legacy_source: "基础/数据结构与算法/刷题总结/双指针.md"
---
#### 双指针

**655. 判断一个数是否为两个数的平方和**

```java
public boolean judgeSquareSum(int c) &#123;
    int i = 0, j = (int) Math.sqrt(c);
    while (i &lt;= j) &#123;
        int sum = i * i + j * j;
        if (sum &lt; c) i++;
        else if (sum > c) j--;
        else return true;
    &#125;
    return false;
&#125;
```

**167. 两数只和2-升序数组**

```java
public int[] twoSum(int[] numbers, int target) &#123;
    if (numbers == null || numbers.length &lt; 2) return new int[]&#123;-1, -1&#125;;
    int left = 0, right = numbers.length - 1;
    while (left &lt; right) &#123;
        int sum = numbers[left] + numbers[right];
        if (sum &lt; target) left++;
        else if (sum > target) right--;
        else return new int[]&#123;left + 1, right + 1&#125;;
    &#125;
    return new int[]&#123;-1, -1&#125;;
&#125;
```

**345. 反转字符串中的元音字符**

```java
private static final Set&lt;Character> set = new HashSet&lt;>(Arrays.asList('a', 'e', 'i', 'o', 'u', 'A', 'E', 'I', 'O', 'U'));
public String reverseVowels(String s) &#123;
    if (s == null || s.length() &lt;= 1) return s;
    int left = 0, right = s.length() - 1;
    char[] cc = s.toCharArray();
    while (left &lt; right) &#123;
        if (!set.contains(s.charAt(left))) left++;
        else if (!set.contains(s.charAt(right))) right--;
        else &#123;
            char temp = cc[left];
            cc[left++] = cc[right];
            cc[right--] = temp;
        &#125;
    &#125;
    return new String(cc);
&#125;
```
**680. 判断一个字符串最多删除一个位置后是否是回文串**

```java
public boolean validPalindrome(String s) &#123;
    int left = 0, right = s.length() - 1;
    while (left &lt; right) &#123;
        if (s.charAt(left) != s.charAt(right)) return valid(s, left, right - 1) || valid(s, left + 1, right);
        else &#123;
            left++;
            right--;
        &#125;
    &#125;
    return true;
&#125;
//----写法2：----
public boolean validPalindrome(String s) &#123;
    for (int left = 0, right = s.length() - 1; left &lt; right; left++, right--) &#123;
        if (s.charAt(left) != s.charAt(right)) return valid(s, left, right - 1) || valid(s, left + 1, right);
    &#125;
    return true;
&#125;


// 判断是否是回文串
private boolean valid(String s, int left, int right) &#123;
    while (left &lt; right) &#123;
        if (s.charAt(left++) != s.charAt(right--)) return false;
    &#125;
    return true;
&#125;
```
**88. 合并两个有序数组**

数组1和2最终合并到1中，1数组的大小正好是m+n；

方法1：开辟一个新数组，全部合并过去，然后转移回nums1;

```java
public void merge(int[] nums1, int m, int[] nums2, int n) &#123;
    int[] nums = new int[m + n];
    int i = 0, j = 0, cnt = 0;
    while (i &lt; m && j &lt; n) &#123;
        if (nums1[i] &lt;= nums2[j]) nums[cnt++] = nums1[i++];
        else nums[cnt++] = nums2[j++];
    &#125;
    while (i &lt; m) nums[cnt++] = nums1[i++];
    while (j &lt; n) nums[cnt++] = nums2[j++];
    for (i = 0; i &lt; m + n; i++) nums1[i]= nums[i];
&#125;
```

方法2：双指针。从后往前合并，防止nums1的元素被覆盖。

```java
public void merge(int[] nums1, int m, int[] nums2, int n) &#123;
    int i = m - 1, j = n - 1, cnt = m + n - 1;
    while (i >= 0 && j >= 0) &#123;
        if (nums1[i] &lt; nums2[j]) nums1[cnt--] = nums2[j--];
        else nums1[cnt--] = nums1[i--];
    &#125;
    while (j >= 0) nums1[cnt--] = nums2[j--];
&#125;

//-----写法2：-----------
public void merge(int[] nums1, int m, int[] nums2, int n) &#123;
    int i = m - 1, j = n - 1, cnt = m + n - 1;
    while (i >= 0 || j >= 0) &#123;
        if (j &lt; 0) break;
        if (i &lt; 0 || nums2[j] > nums1[i]) nums1[cnt--] = nums2[j--];
        else nums1[cnt--] = nums1[i--];
    &#125;
&#125;
```

**141. 判断链表是否有环**

方法1：HashSet判重。
```java
public boolean hasCycle(ListNode head) &#123;
    if (head == null) return false;
    Set&lt;ListNode> set = new HashSet&lt;>();
    ListNode p = head;
    while (p != null) &#123;
        if (!set.add(p)) return true;
        p = p.next;
    &#125;
    return false;
&#125;
```
方法2：快慢指针总会相遇。
```java
public boolean hasCycle(ListNode head) &#123;
    if (head == null) return false;
    ListNode slow = head, fast = head;
    while (fast != null && fast.next != null) &#123;
        if (slow == fast) return true;
        slow = slow.next;
        fast = fast.next.next;
    &#125;
    return false;
&#125;
```
**142. 判断链表是否成环，如果成，返回那个节点**

方法1：hash表对比

方法2：快慢指针。

```java
public ListNode detectCycle(ListNode head) &#123;
    if (head == null) return null;
    ListNode slow = head, fast = head;
    while (fast != null && fast.next != null) &#123;
        slow = slow.next;
        fast = fast.next.next;
        if (slow == fast) break;
    &#125;
    if (fast == null || fast.next == null) return null;
    // 此时快指针走了2n,慢指针走了n
    // fast: s + nc + p, slow: s + mc + p
    // (n - m) c = s + mc + p -> s + p = kc
    // 所以当前在p点的指针再走s步就是kc，刚好成环,再次相交
    // 所以一个指针从头再走s到交界点，另一个在交界点再走s又回到交界点
    fast = head;
    while (slow != fast) &#123;
        slow = slow.next;
        fast = fast.next;
    &#125;
    return slow;
&#125;
```
**11. 盛水最多的容器**

求最大面积。

方法1：两层for循环暴力法。

```java
// 方法1：暴力法。
public int maxArea(int[] height) &#123;
    int n = height.length;
    int max = 0;
    for (int i = 0; i &lt; n - 1; i++) &#123;
        for (int j = i + 1; j &lt; n ; j++) &#123;
            int cur = (j - i) * Math.min(height[i], height[j]);
            max = Math.max(max, cur);
        &#125;
    &#125;
    return max;
&#125;
```

方法2：双指针

```java
// 方法2：双指针
public int maxArea(int[] height) &#123;
    int n = height.length;
    int left = 0, right = n - 1;
    int res = 0;
    while (left &lt; right) &#123;
        if (height[left] &lt; height[right]) &#123;
            res = Math.max(res, (right - left) * height[left]);
            left++;
        &#125; else &#123;
            res = Math.max(res, (right - left) * height[right]);
            right--;
        &#125;
    &#125;

    return res;
&#125;

//-------------写法优化-------

public int maxArea(int[] height) &#123;
    int n = height.length;
    int res = 0;
    int left = 0, right = n - 1;
    while (left &lt; right) 
        res = Math.max(res, (right - left) * (height[left] &lt; height[right] ? height[left++] : height[right--]));
    return res;
&#125;
```

**15. 三数之和**

```java
public List&lt;List&lt;Integer>> threeSum(int[] nums) &#123;
    if (nums == null || nums.length &lt; 3) return new ArrayList&lt;>(0);
    List&lt;List&lt;Integer>> res = new ArrayList&lt;>();
    Arrays.sort(nums);
    int n = nums.length;
    for (int i = 0; i &lt; n - 2; i++) &#123;
        // 去重
        if (i > 0 && nums[i] == nums[i - 1]) continue;
        int left = i + 1, right = n - 1;
        while (left &lt; right) &#123;
            int sum = nums[i] + nums[left] + nums[right];
            if (sum &lt; 0) left++;
            else if (sum > 0) right--;
            else &#123;
                res.add(Arrays.asList(nums[i], nums[left], nums[right]));
                left++;
                right--;
                while (left &lt; right && nums[left] == nums[left + 1]) left++;
                while (left &lt; right && nums[right] == nums[right - 1]) right--;
            &#125;
        &#125;
    &#125;
    return res;
&#125;
```
