---
title: "重构"
description: "重构 的历史学习笔记。"
date: 2026-05-21
tags: ["设计模式", "架构设计"]
keywords: ["设计模式", "架构设计", "重构"]
legacy_source: "基础/设计模式/设计原则/重构.md"
---
#### 重构


重构就是发现代码质量问题，并且对其进行优化的过程

**what**
- 大规模高层次重构包括对代码分层、模块化、解耦、梳理类之间的交互关系、抽象复用组件等等。这部分工作利用的更多的是比较抽象、比较顶层的设计思想、原则、模式。


- 小规模低层次的重构包括规范命名、注释、修正函数参数过多、消除超大类、提取重复代码等等编程细节问题，主要是针对类、函数级别的重构。小规模低层次的重构更多的是利用编码规范这一理论知识。


**how**

- 持续重构，防止代码出现很大问题。


- 需要分阶段小规模重构，不断迭代。

**有效的方法**：

1. 通过写单元测试:

(1) 为什么写单元测试可以有效促进重构：

如果一个方法很难编写单元测试，说明设计不合理，耦合过高，代码测试下不好。

(2) 编写单元测试的过程：

先按照正常测试流程编写，如果发现依赖静态方法或者其他接口，需要先进行重构。

对于无权限更改的静态方法，可以单独开辟出一个类，使用非静态方法来替代；

对于代码中使用其他接口的地方，可以使用依赖注入的方式，将注入逻辑交给调用者或框架来解决；

对于代码中包含未决行为的逻辑，抽象出简单的逻辑函数；

编写测试框架时，对于依赖外部或未决的方法，通过上面的方法重构以后，就可以使用mock的方式来解决。

（3）如何mock：

对于依赖外部的接口，我们可以继承并重写需要用到的方法进行mock测试；

对于重构时抽出的未决行为的方法，也可以在测试时重写实现方法来测试。

2. 常见的 Anti-Patterns：

常见的测试不友好的代码有下面这 5 种：

代码中包含未决行为逻辑

滥用可变全局变量

滥用静态方法

使用复杂的继承关系

高度耦合的代码

3. 通过解耦：


如何解耦：

封装与抽象、中间层、模块化，以及一些其他的设计思想与原则，比如：单一职责原则、基于接口而非实现编程、依赖注入、多用组合少用继承、迪米特法则等。当然，还有一些设计模式，比如观察者模式。
